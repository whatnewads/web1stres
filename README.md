
  # Delegate Orchestrator Tasks

  This is a code bundle for Delegate Orchestrator Tasks. The original project is available at https://www.figma.com/design/qb414J1M9XGU8DQnOpTcI0/Delegate-Orchestrator-Tasks.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  