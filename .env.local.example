# Formspree — schedule-consult form (server-side only)
FORMSPREE_FORM_ID=your_formspree_form_id_here

# CSRF secret — used to sign double-submit tokens (server-side only)
# Generate a random 32+ character string
CSRF_SECRET=replace_with_random_secret_string
